# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently a bare scaffold: it contains only `README.md` (the project title "Football-Scout") with no source code, build tooling, dependency manifest, or tests yet. There is no language, framework, or architecture established.

When the project's direction is chosen, update this file with:

- **Build / run / test commands** — including how to run a single test — once a build system or package manager is added.
- **Architecture** — the big-picture structure (modules, data flow, external services) that spans multiple files and isn't obvious from a directory listing.

Until then, there are no project-specific commands or conventions to follow beyond standard git usage.
